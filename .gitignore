# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
*.tsbuildinfo

# environment — never commit real secrets; .env.example is the template
.env
.env.*
!.env.example

# prisma generated client (regenerated by `pnpm db:generate`)
apps/api/src/generated/

# test / coverage
coverage/
.vitest/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# AI agent tooling — design-skill references and their lockfile, plus the raw
# brand source files (the copies actually used by the app live under
# apps/web/public/, already committed). Not project source; kept local only.
.agents/
DESIGN.md
skills-lock.json
/brand.png
/favicon.ico
